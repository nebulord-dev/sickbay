name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  placeholder:
    name: CI Disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI is temporarily disabled"
