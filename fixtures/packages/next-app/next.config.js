// Intentional: response header customization is not configured.
// This triggers the next-security-headers check.
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
